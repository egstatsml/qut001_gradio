# Versions are pinned deliberately. Gradio makes breaking changes between major
# releases, so an unpinned install can change behaviour between semesters and
# break demos that worked last term.
#
# To move to newer versions: bump the numbers here, rebuild, and check each app.
gradio==6.26.0
fastapi==0.141.1
uvicorn[standard]==0.52.1
numpy>=2.4,<3
matplotlib>=3.11.1,<4
pandas>=3.0.5,<4
transformers>=5.14.1,<6
notebook>=7.6.1,<8
jupyterlab>=4.6.2,<5
ipywidgets>=8.1.8,<9
scikit-learn>=1.4
